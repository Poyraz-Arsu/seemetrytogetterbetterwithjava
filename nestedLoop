Starting work on the side made me slow down on my java journey.Im back on it again from where i left which would be the 2d arrays.





public class randomNum {
    public static void main(String[] args) {
        
        int [][] newArray = new int[100][10];

       for(int i = 0 ; i< newArray.length ; i++){
        for(int j = 0 ; j< newArray[j].length ; j++){
            newArray[i][j] = (int)((Math.random()*100)+1) ;
            System.out.print(newArray[i][j]+ " ");
        }
        System.out.println(" ");          } 
        
     
   
   
   
    }

    public static int numgenerator(){
    int randomNum   = (int) ((Math.random()*100)+1);
        return randomNum;
}

}
